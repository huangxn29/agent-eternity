# 断点续传、守护进程、P1问题与运维修复记录

## P1问题清单→Issues #7-#10
- #7 Gateway自恢复(镇元/P0) → ✅ done（2026-05-17确认）
- #8 web_search(砺元/P0) → 🔄 in-progress（DuckDuckGo不被识别，需openclaw configure交互式配置）
- #9 P1b验收重写(筑元/P1) → ⏳ pending
- #10 执行计划v3(筑元/P1) → ⏳ pending

## 关键修复历程
- **容器重启三根因**：workspace路径重置/BOOTSTRAP重现/Token模型变化→根因：entrypoint.sh覆盖openclaw.json
- **post_start_fix.sh**：v1.0用declare -A关联数组→v1.1改用case语句（bash兼容）+model改为sonnet
- **守护进程**：v1.0(docker exec不可靠)→v1.3(docker restart+/health端点30秒检测)
- **web_search**：duckduckgo provider不被识别，残留配置致Gateway启动失败，需jq清理

## 2026-05-17 模型切换与唤醒修复
- **模型从gpt-oss-120b→sonnet**：gpt-oss-120b工具调用不可靠（agent只描述不执行exec），sonnet调工具稳定
- **post_start_fix.sh v1.1**：declare -A关联数组在容器bash中报"bad array subscript"→用case替代+model改sonnet
- **唤醒经验**：jq -Rs构造长--params致Gateway失败→写params到临时文件`--params "$(cat /tmp/params.json)"`
- **P2 Sprint 1进展**：commit 79d2f984 "feat: 完成Agent Eternity完整代码实现"（challenge.py等），Issue #11未关闭
- **砺元安全审计**：9次工具调用但容器内找不到Python源码（git clone不包含app/目录），需确认代码部署位置

## 技能版本更新
- agent-awake v3.2→v3.3（断点续传+守护进程+唤醒v2.4）
- agent-deploy v1.8→v1.9（守护进程部署+post_start_fix）
